# vsix extensions

VS Code extensions in this repository.

## Projects

- **[fabric-workspace-compare](./fabric-workspace-compare/)** — Read-only Microsoft Fabric workspace comparison tool (notebooks and data pipelines). Build the installable `.vsix` with `npm run package` inside that folder.
